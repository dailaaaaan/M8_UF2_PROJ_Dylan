module.exports = {
  // Dirección del servidor MySQL (por defecto es localhost)
  HOST: "localhost",
  // Usuario de la base de datos (por defecto en local suele ser "root")
  USER: "root",
  // Contraseña de la base de datos (vacía en muchos casos para entornos de desarrollo local)
  PASSWORD: "",
  // Nombre de la base de datos a la que nos conectamos
  DB: "library_db"
};
